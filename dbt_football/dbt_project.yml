name: 'football_db'
version: '1.0.0'
config-version: 2

profile: 'football_db'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  football_db:
    staging:
      +materialized: view
    marts:
      +materialized: table
